Account secrets and passwords necessary for authentication stored as a local .env file.

To run, just execute postEvents.py from the command line and follow user input prompts to post events to social media. Events and media assets are downloaded from the website at runtime, so the code always uses the latest event library.

Currently just posts to Twitter and Reddit.
